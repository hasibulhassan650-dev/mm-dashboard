{
  "permissions": {
    "allow": [
      "PowerShell(Get-Command streamlit)",
      "PowerShell(Get-Process | Where-Object { $_.MainWindowTitle -match \"streamlit\" -or $_.ProcessName -match \"streamlit\" } | Select-Object Id, ProcessName, MainWindowTitle)",
      "PowerShell(netstat -ano)"
    ]
  }
}
